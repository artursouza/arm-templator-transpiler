input namePrefix string

// custom provider which knows how to handle yaml
// (treated as verbatim text string with interpolation)
resource yamlProvider 'yamlConfig' yamlConfig ```
 - yamlProp
 - otherYamlProp
   - ${namePrefix}-interpolated
```

// custom provider outputs can be accessed
output someConfig: yamlConfig.someOutput